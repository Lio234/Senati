@model app_compuware.Models.Cliente

@{
    ViewData["Title"] = "Listado de Clientes";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section EstiloDT {
    <link rel="stylesheet" href="~/lib/datatable/css/jquery.dataTables.min.css" />
}

<h1>Listado de los Clientes</h1>
<hr />

<div class="container">
    <div class="row mb-3">
    </div>

    <br />

    <div class="row">
        <table id="tb_cliente" class="table table-bordered table-striped display" style="width: 100%;">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(client => client.id_cliente)</th>
                    <th>@Html.DisplayNameFor(client => client.nombre)</th>
                    <th>@Html.DisplayNameFor(client => client.ap_paterno)</th>
                    <th>@Html.DisplayNameFor(client => client.ap_materno)</th>  <!-- Nuevo campo -->
                    <th>@Html.DisplayNameFor(client => client.direccion)</th>  <!-- Nuevo campo -->
                    <th>@Html.DisplayNameFor(client => client.telefono)</th>
                    <th>@Html.DisplayNameFor(client => client.correo)</th>  <!-- Nuevo campo -->

                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>
</div>


<!-- Modal para agregar/editar cliente -->
<div class="modal" tabindex="-1" id="modalCliente">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Agregar Nuevo Cliente</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <form id="formCliente" method="post">
                    <div class="mb-3">
                        <label for="id_cliente" class="form-label">ID Cliente</label>
                        <input type="text" class="form-control" asp-for="id_cliente" required>
                    </div>
                    <div class="mb-3">
                        <label for="nombre" class="form-label">Nombre</label>
                        <input type="text" class="form-control" asp-for="nombre" required>
                    </div>
                    <div class="mb-3">
                        <label for="ap_paterno" class="form-label">Apellido Paterno</label>
                        <input type="text" class="form-control" asp-for="ap_paterno" required>
                    </div>
                    <div class="mb-3">
                        <label for="ap_materno" class="form-label">Apellido Materno</label> <!-- Nuevo campo -->
                        <input type="text" class="form-control" asp-for="ap_materno" required>
                    </div>
                    <div class="mb-3">
                        <label for="telefono" class="form-label">Teléfono</label>
                        <input type="text" class="form-control" asp-for="telefono" required>
                    </div>
                    <div class="mb-3">
                        <label for="correo" class="form-label">Correo Electrónico</label> <!-- Nuevo campo -->
                        <input type="email" class="form-control" asp-for="correo" required>
                    </div>
                    <div class="mb-3">
                        <label for="direccion" class="form-label">Dirección</label> <!-- Nuevo campo -->
                        <input type="text" class="form-control" asp-for="direccion" required>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
                <button type="button" class="btn btn-primary" id="btnSave">Guardar</button>
            </div>
        </div>
    </div>
</div>

<a asp-action="ExportarPDF" class="btn btn-primary">
    <i class="fas fa-file-pdf"></i> Exportar PDF
</a>
<a asp-action="ExportarExcel" class="btn btn-success">
    <i class="fas fa-file-excel"></i> Exportar a Excel
</a>

<!-- Modal de confirmación de eliminación -->
<div class="modal" tabindex="-1" id="modalEliminar">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Confirmar Eliminación</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p>¿Estás seguro de que deseas eliminar este cliente?</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
                <button type="button" class="btn btn-danger" id="btnEliminarConfirmado">Eliminar</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/lib/datatable/js/jquery.dataTables.js"></script>
    <script src="~/lib/datatable/js/dataTables.buttons.min.js"></script>
    <script>
                $(document).ready(function () {
            // Inicializamos DataTables para la tabla de clientes
            $("#tb_cliente").DataTable({
                "ajax": {
                    "url": "@Url.Action("Listar", "Cliente")",  // Acción para listar los clientes
                    "type": "GET",
                    "dataType": "json"
                },
                "columns": [
                    { "data": "id_cliente" },
                    { "data": "nombre" },
                    { "data": "ap_paterno" },
                    { "data": "ap_materno" },  // Nuevo campo
                    { "data": "direccion" },   // Nuevo campo
                    { "data": "telefono" },
                    { "data": "correo" },  // Nuevo campo
                    {
                        "data": "id_cliente",
                        "render": function (data) {
                            return "<button class='btn btn-outline-success' type='button' onclick='AbrirRegistro(\"" + data + "\")'><i class='far fa-edit'></i> Editar</button>" +
                                "<button class='btn btn-outline-danger' type='button' onclick='Eliminar(\"" + data + "\")'><i class='far fa-trash-alt'></i> Borrar</button>";
                        }
                    }
                ],
                "order": [[1, "asc"]],
                dom: "Bfrtip",
                buttons: [
                    {
                        text: "Registrar Cliente",  // Este botón abre el modal de registro
                        attr: { class: "btn btn-outline-primary" },
                        action: function () {
                            AbrirRegistro("");  // Acción para abrir el modal vacío
                        }
                    }
                ]
            });
        });

        // Función para abrir el modal de agregar/editar cliente
        function AbrirRegistro(id_cliente) {
            if (id_cliente != "") {
                // Si existe el cliente, cargamos los datos
                jQuery.ajax({
                    url: "@Url.Action("Consultar", "Cliente")" + "?id_cliente=" + id_cliente,  // Acción para consultar los datos del cliente
                    type: "GET",
                    dataType: "json",
                    success: function (data) {
                        if (data != null) {
                            $("#id_cliente").val(data.id_cliente).attr("readonly", true);  // Deshabilitamos el campo ID
                            $("#nombre").val(data.nombre);
                            $("#ap_paterno").val(data.ap_paterno);
                            $("#telefono").val(data.telefono);
                            $("#correo").val(data.correo);
                            $("#direccion").val(data.direccion);
                        }
                    }
                });

                $(".modal-title").text("Editar Cliente");  // Título del modal
                $("#btnSave").html("<i class='fas fa-save'></i> Guardar cambios");  // Texto del botón
            } else {
                // Si es nuevo registro, limpiamos los campos
                $("#id_cliente").attr("readonly", false);
                $("#id_cliente").val("");
                $("#nombre").val("");
                $("#ap_paterno").val("");
                $("#telefono").val("");
                $("#correo").val("");
                $("#direccion").val("");
                $(".modal-title").text("Registrar Cliente");
                $("#btnSave").html("<i class='fas fa-save'></i> Guardar datos");
            }
            $("#modalCliente").modal("show");  // Mostramos el modal
        }

        // Función para guardar el cliente
        $("#btnSave").click(function () {
            let cliente = {
                "id_cliente": $("#id_cliente").val(),
                "nombre": $("#nombre").val(),
                "ap_paterno": $("#ap_paterno").val(),
                "telefono": $("#telefono").val(),
                "correo": $("#correo").val(),
                "direccion": $("#direccion").val()
            };

            $.ajax({
                url: "@Url.Action("Grabar", "Cliente")",  // Acción para guardar el cliente
                type: "POST",
                data: JSON.stringify(cliente),
                contentType: "application/json; charset=utf-8",
                dataType: "json",
                success: function (data) {
                    if (data.resultado) {
                        $("#tb_cliente").DataTable().ajax.reload();  // Recargamos la tabla
                        $("#modalCliente").modal("hide");  // Cerramos el modal
                    } else {
                        alert("No se pudo guardar la información.");
                    }
                },
                error: function () {
                    alert("Error al guardar la información.");
                }
            });
        });

        // Función para eliminar un cliente
        let clienteAEliminar = "";

        function Eliminar(id_cliente) {
            clienteAEliminar = id_cliente;
            $("#modalEliminar").modal("show");  // Mostramos el modal de eliminación
        }

        $("#btnEliminarConfirmado").click(function () {
            $.ajax({
                url: "@Url.Action("Borrar", "Cliente")",  // Acción para eliminar el cliente
                type: "POST",
                data: { id_cliente: clienteAEliminar },
                success: function (data) {
                    if (data.resultado) {
                        $("#tb_cliente").DataTable().ajax.reload();  // Recargamos la tabla
                        $("#modalEliminar").modal("hide");  // Cerramos el modal de eliminación
                    } else {
                        alert("No se pudo eliminar el cliente.");
                    }
                },
                error: function () {
                    alert("Error al eliminar el cliente.");
                }
            });
        });
    </script>
}
